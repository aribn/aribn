name: aribn

on:
  schedule:
    # Once a day at 8 AM
    - cron: '5 * * * *'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: aribn/rss-to-readme@v1
        with:
          feed-url: https://favorites.aribadernatal.com/feed/
          readme-section: feed
